name: "sync-template"
description: "Synchroniser les workflows Claude Code vers le repo template externe. Nettoie automatiquement les references projet."
version: "1.0.0"
created: "2025-01-24"
type: "task-simple"

cc_features:
  context_fork: false
  subagents: false
  hooks: false
  model: "haiku"
  user_invocable: true
  disable_model_invocation: true

files:
  skill: "SKILL.md"
  references:
    - "references/cleaning-rules.md"

dependencies: []

patterns_applied:
  - "A3: Two Content Types (Task Content)"
  - "E1: Model Strategy (haiku for simple ops)"
  - "Q1: APEX Self-Validation"
  - "R1: Fallback Strategies"
  - "S1: Sandwich Structure"
  - "I3: Minimal User Interaction"

notes: |
  Generated by /create-workflow v3

  This skill syncs Claude Code configuration from the source project
  to an external template repository, cleaning project-specific
  references to keep the template generic.

  Key features:
  - Auto-detection of files to sync
  - Pattern-based cleaning of project references
  - Dry-run mode for preview
  - Optional auto-push to remote
